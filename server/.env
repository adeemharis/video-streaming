# Server
PORT=5000
JWT_SECRET=supersecretjwt

# MongoDB (hosted in Docker, accessed from host)
MONGODB_URI=mongodb://localhost:27017/videoapp

# Neo4j (hosted in Docker, accessed from host)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=neo4jpassword

# Redis (hosted in Docker, accessed from host)
REDIS_URL=redis://localhost:6379

# MinIO / S3-compatible storage (hosted in Docker, accessed from host)
S3_BUCKET=videos-bucket
S3_ENDPOINT=http://localhost:9000
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin

COOKIE_NAME=vm_access
# in production set NODE_ENV=production and configure CORS_ORIGIN accordingly
CORS_ORIGIN=http://localhost:5173